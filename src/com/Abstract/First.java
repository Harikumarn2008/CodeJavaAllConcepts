package com.Abstract;

public interface First extends Second {

	/*In Interface the variable it consider the public,static,final so 
	we should initialize the value we can't the declaration only without
	initialize*/
	String companyName = "MK Pvt.ltd"; 
	int age = 1;
	
	void method1();
	void method2();
	
	
}
